
-- DROP TABLE IF EXISTS inventory;

CREATE TABLE inventory (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    category VARCHAR(100),
    current_quantity INTEGER DEFAULT 0,
    min_required_quantity INTEGER DEFAULT 10,
    price DECIMAL(10, 2),
    sales_history JSONB DEFAULT '[]',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- Sample Data for Verification
INSERT INTO inventory (name, category, current_quantity, min_required_quantity, price, sales_history)
VALUES 
('AWS EC2 Reserved Instance', 'Compute', 50, 20, 1500.00, '[{"date": "2024-05-01", "quantity": 2}]'),
('S3 Bucket (Provisioned)', 'Storage', 5, 10, 45.00, '[{"date": "2024-05-02", "quantity": 1}]');
